import LoginForm from "./components/LoginForm";
/**
 * Create a fresh Vue Application instance
 */
import Vue from 'vue';

new Vue({
    el: '#loginBloc',
    components: {
        LoginForm
    }
});